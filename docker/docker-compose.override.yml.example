services:
  postgres:
    ports:
      - '127.0.0.1:5432:5432'

  pg-admin:
    ports:
      - '127.0.0.1:5050:80'

  pl-app:
    ports:
      - '127.0.0.1:8000:8000'

networks:
  pl-dev-network:
    driver: bridge